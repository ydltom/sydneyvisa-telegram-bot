# Telegram bot credentials
TELEGRAM_TOKEN=
CHAT_ID=

# MigrateMate API cookie (grab from Chrome DevTools → Network tab → "cookie" request header)
# This expires periodically — refresh it when you start getting 429s again.
MIGRATEMATE_COOKIE=
